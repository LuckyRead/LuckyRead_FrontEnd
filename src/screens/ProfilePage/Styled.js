import styled from "styled-components";
import { Palette } from "../../themes/colors";

const ProfileContainer = styled.div``;

const ProfileLeftInfo = styled.div``;
const ProfileRightInfo = styled.div``;

export { ProfileContainer, ProfileLeftInfo, ProfileRightInfo };
